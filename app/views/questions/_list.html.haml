%table
  - questions.each do |question|
    %tr
      %td= link_to question.question, question
      %td= question.user.login if question.user
      %td= link_to 'Edit', edit_question_path(question) if can? :edit, question
      %td= link_to 'Destroy', question, :method => :delete, :data => { :confirm => 'Are you sure?' } if can? :destroy, question